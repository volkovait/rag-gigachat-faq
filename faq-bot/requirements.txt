# Нужен Python 3.10+. Не используйте системный pip от Python 2.7.
# Установка:
#   cd faq-bot && python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

httpx>=0.27,<1
python-dotenv>=1.0,<2
