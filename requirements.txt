# Python 3.8+. Зависимости ставьте в venv (обязательно для Homebrew / PEP 668):
#
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#   python server.py
#
# «pip install» без venv на системном python3 часто даёт externally-managed-environment.

httpx>=0.27,<1
python-dotenv>=1.0,<2
